import { ValidationPipe } from '@nestjs/common';
/** Global validation pipe producing structured field errors for the problem-details filter. */
export declare function createValidationPipe(): ValidationPipe;
