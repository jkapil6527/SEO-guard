{
  "name": "@seo-guardian/crawler-core",
  "version": "0.1.0",
  "private": true,
  "description": "I/O primitives for crawling: SSRF-guarded fetcher, robots.txt, sitemaps, URL normalization. No business rules.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@seo-guardian/shared": "workspace:*"
  },
  "devDependencies": {
    "@seo-guardian/config": "workspace:*",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.29",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  }
}
