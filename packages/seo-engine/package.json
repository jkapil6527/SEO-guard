{
  "name": "@seo-guardian/seo-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Pure Technical-SEO validation engine: artifact extraction, check registry, scoring. Zero I/O.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@seo-guardian/shared": "workspace:*",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@seo-guardian/config": "workspace:*",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.29",
    "domhandler": "^5.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  }
}
